<div class="container">
  <div class="row justify-content-center">
    <div class="col-10">
        <%= render 'shared/errors', obj:@asset_type %>
        <%= form_with(model: @asset_type, class: "shadow p-3 mb-3 bg-info rounded", local: true) do |f| %>
          <div class="form-group row">
            <%= f.label :asset_type, class: "col-2 col-form-label text-light" %>
              <div class="col-10">
                <%= f.text_field :asset_type, class: "form-control shadow rounded", placeholder: "Enter an asset_type (Desktop, Laptop etc)" %>
              </div>
          </div>
          <div class="form-group row">
            <%= f.label :comment, class: "col-2 col-form-label text-light" %>
              <div class="col-10">
                <%= f.text_field :comment, class: "form-control shadow rounded", placeholder: "Enter a comment" %>
              </div>
          </div>
          <div class="form-group row justify-content-center">
            <%= f.submit(@asset_type.new_record? ? "Create" : "update account", class: "btn btn-outline-light btn-lg") %>
          </div>
        <% end %>
    </div>
  </div>
</div>
